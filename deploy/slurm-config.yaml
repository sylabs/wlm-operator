apiVersion: v1
kind: ConfigMap
metadata:
  name: slurm-config
  namespace: default
data:
  config: |
    minikube:
      slurm_ssh: my.slurm.ssh:2222
      slurm_local: localhost:8080
      resources:
        cpu: 2
      labels:
        cuda: 10.0
        containers: singularity
