apiVersion: v1
kind: ConfigMap
metadata:
  name: slurm-config
  namespace: default
data:
  config: |
    minikube:
      red_box_addr: http://localhost:8080
      resources:
        cpu: 2
      labels:
        cuda: 10.0
        containers: singularity
